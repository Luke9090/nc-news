import React from 'react';
import loading from '../imgs/loading.svg';

function Loading() {
  return <img src={loading} alt="Loading icon" width="200px" className="loadingImg" />;
}

export default Loading;
